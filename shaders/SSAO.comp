//GLSL version to use
#version 460
//size of a workgroup for compute
layout (local_size_x = 16, local_size_y = 16) in;

layout (set=0, binding=0) uniform image2D inNormal;
layout (set=0, binding=1) uniform image2D inDepth;
layout (set=0, binding=2) uniform image2D inWorldPos;

void main() 
{
	outFragColor = vec4(1.0f, 0.0f, 0.0f,1.0f);
}